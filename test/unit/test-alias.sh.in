#!@ZSH_PROG@
# -*- shell-script -*-

test_alias()
{
    _Dbg_add_alias q quit
    _Dbg_expand_alias q
    assertEquals 'quit' $expanded_alias
    _Dbg_remove_alias q
    _Dbg_expand_alias q
    assertEquals 'q' $expanded_alias
}

# Make sure @top_builddir@ has a trailing slash
if [ '@top_builddir@' = '' ] ; then
  echo "Something is wrong top_builddir is not set."
 exit 1
fi
top_builddir=@top_builddir@
top_builddir=${top_builddir%%/}/

. $top_builddir/lib/alias.sh
. $top_builddir/lib/help.sh
. $top_builddir/command/alias.sh

# load shunit2
set -o shwordsplit
SHUNIT_PARENT=$0

srcdir=@srcdir@
srcdir=${srcdir}/
. ${srcdir}/shunit2
